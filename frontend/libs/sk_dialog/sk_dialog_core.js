class SK_Dialog {
    constructor(){

    }

    open(opt){
        return sk_dialog.open(opt)
    }

    save(opt){
        return sk_dialog.save(opt)
    }

    message(opt){
        return sk_dialog.message(opt)
    }
}